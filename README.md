There is a web app with some rooms, where you can communicate with others in real-time. There were used websockets to make it work.

To install all necassery libs enter:
**pip install -r requirements.txt**
